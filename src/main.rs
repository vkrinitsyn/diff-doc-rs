// use diff_doc::*;

/**
usage examples

*/

fn main() {
    // let m = Mismatch::new( &json!({"a":["b","c"], "d":["e","f"]}),
    //                           &json!({"a":["b","c"], "d":["e","g"]})).unwrap();
    // println!("{}", m);

}


#[cfg(test)]
mod tests {
    #[test]
    fn test_compile() {
        assert!(true);
    }
}